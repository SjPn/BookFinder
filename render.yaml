services:
  - type: web
    name: bookfinder
    runtime: docker
    plan: starter
    region: frankfurt
    healthCheckPath: /api/stats
    envVars:
      - key: PYTHONPATH
        value: /app/src
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
